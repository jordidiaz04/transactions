server:
  port: '8083'

spring:
  application:
    name: transaction
  config:
    import: optional:configserver:http://localhost:9000
  cloud:
    config:
      profile: dev

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    shutdown:
      enabled: 'true'
    restart:
      enabled: 'true'